package com.study2;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;
//@~Controller : 객체생성,역할(MVC)
@RestController
public class NowController {
	
	@GetMapping("/now")
	public Now now() {
		return new Now();	//1	
	}
}
